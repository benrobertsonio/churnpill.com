---
// Solution component showing how Churnpill solves the churn problem

export interface Props {
  subtitle?: string;
  title?: string;
  features?: Array<{
    title: string;
    description: string;
  }>;
  pricingText?: string;
  ctaText?: string;
  ctaUrl?: string;
}

const {
  subtitle = "The solution",
  title = "ChurnPill makes it simple 💊",
  features = [
    {
      title: "🔌 Plug-and-play cancel flow for Stripe",
      description: "Intercept cancellations instantly with a snippet — no custom engineering required."
    },
    {
      title: "🔄 Pause, downgrade, or discount — built in",
      description: "Offer customers alternatives to canceling with configurable offboarding flows."
    },
    {
      title: "🔍 Know what's working",
      description: "Track cancel reasons and see which offers actually save revenue."
    },
    {
      title: "💰 Clear ROI from day one",
      description: "Your dashboard shows exactly how much revenue you've recovered each month."
    }
  ],
  pricingText = "All starting at just $49/month — affordable, simple, and ROI-positive.",
  ctaText = "Get started",
  ctaUrl = "#"
} = Astro.props;
---

<div class="bg-gray-50 py-24 sm:py-32 dark:bg-gray-800">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-2xl lg:text-center">
      <h2 class="text-base/7 font-semibold text-pink-600 dark:text-pink-400 font-archivo">{subtitle}</h2>
      <p class="mt-2 text-4xl font-semibold tracking-tight text-pretty text-gray-900 sm:text-5xl lg:text-balance dark:text-white font-gambarino">{title}</p>
    </div>
    <div class="mx-auto mt-16 max-w-2xl sm:mt-20 lg:mt-24 lg:max-w-none">
      <dl class="grid max-w-xl grid-cols-1 gap-x-8 gap-y-16 lg:max-w-none lg:grid-cols-2">
        {features.map((feature) => (
          <div class="flex flex-col">
            <dt class="text-2xl font-semibold text-gray-900 dark:text-white font-gambarino">
              {feature.title}
            </dt>
            <dd class="mt-4 text-base/7 text-gray-600 dark:text-gray-400 font-archivo">
              <p class="flex-auto">{feature.description}</p>
            </dd>
          </div>
        ))}
      </dl>
      
      <div class="mt-16 text-center">
        <p class="text-lg text-gray-600 dark:text-gray-300 font-archivo mb-8">
          {pricingText}
        </p>
        <a href={ctaUrl} class="inline-flex items-center gap-x-2 rounded-md bg-pink-600 px-6 py-3 text-sm/6 font-semibold text-white shadow-sm hover:bg-pink-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-pink-600 font-archivo">
          {ctaText} <span aria-hidden="true">→</span>
        </a>
      </div>
    </div>
  </div>
</div>